- execution for SDD and requirements cli should be the same

- add a AI config for the requirements the same as SDD and save them separately in config.json

- new spec manually:
    - only draft state
    - tags are not passing to the spec file
    - tags are parsing as bullet points (-) instead of coucelled list ([])
    - agent skills 